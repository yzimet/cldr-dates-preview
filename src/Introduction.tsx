import React from "react";
import Typography from "@material-ui/core/Typography";

export default function Introduction() {
  return (
    <Typography>
      Use the input fields below to set the Locale, Date, and Time.
    </Typography>
  );
}
